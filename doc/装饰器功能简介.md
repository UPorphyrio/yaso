装饰器作用简介

```typescript
@Get(paht:string)  //标注方法为处理对应路径的`get`请求的处理函数  
@Post(path:string) //标注方法为处理对应路径的`post`请求的处理函数  
 @Route(path:string) //标注类为处理对应的基础路由的处理类，如果 path 为空，则对应的处理路径为类的名称
@HttpHead(config:httpHeaderConfigObj) //注入http头 

```
